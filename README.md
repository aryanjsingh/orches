# Orches

macOS menubar app — AI credit proxy (WIP).

## Requirements

- macOS 13+
- Swift 5.9+ (comes with Xcode 15+ or `xcode-select --install`)

## Run

```bash
swift build -c release
.build/release/Orches
```

## Dev

```bash
swift run
```
